// Copy this file to config.local.js and paste your YouTube Data API v3 key.
// config.local.js is gitignored so the key never leaves your machine.
// Without it, the app falls back to the activeMonths heuristic.
window.WWOB_CONFIG = {
  youtubeApiKey: 'AIza...',
};
